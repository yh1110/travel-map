# travel-map — Claude向けプロジェクトルール

作業を始める前に、`.claude/rules/` 配下のファイルを必ず全て読むこと。

- `.claude/rules/physical-device-testing.md` — iPhone実機での動作確認前に必ず確認する手順(Metroのポート・.envのSupabase URL)

`README.md`(セットアップ手順・データモデル)、`AGENTS.md`(Expo SDKバージョンに関する注意)も参照すること。
